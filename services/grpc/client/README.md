# gRPC Client (Node.js)

Cliente gRPC para validar tickets.

## Rodar

npm install
node client.js

## Variaveis

- GRPC_HOST (default: localhost)
- GRPC_PORT (default: 50051)
- TICKET_ID (default: TCK-123)
- EVENT_ID (default: evt-1)
